import packageJson from "../../package.json";

export const APP_NAME = "Project Huginn";
export const APP_VERSION = packageJson.version;
export const APP_VERSION_LABEL = `${APP_NAME} - v${APP_VERSION}`;
